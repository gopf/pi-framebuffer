#!/bin/sh

git config --global alias.st "status -s"
git config --global alias.ci commit
git config user.name "gopf"
git config user.email "44543690+gopf@users.noreply.github.com"
git config credential.helper store
